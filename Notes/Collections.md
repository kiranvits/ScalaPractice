#### List:

In scala List is an immutable in nature. We can not alter the list once created but can create new list by appending 
elements at the start of the List. 

1. Create immutable list

    ```
    scala> val list = List(1,2,3)
    list: List[Int] = List(1, 2, 3)
    ```
2. add new element
    